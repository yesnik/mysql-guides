# Installation

## Centos

Install packets:

```bash
sudo yum install mariadb-server
```

Enable service:

```bash
sudo service mariadb start
```

Run config script:

```bash
sudo mysql_secure_installation
```
